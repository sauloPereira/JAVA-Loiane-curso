package com.loiane.java.aula41;

public class OOHerancaClasseAbstrata {
	/*
	 * Quando uma classe é abstract, não pode ser instanciada diretamente
	 * apenas pode ser estendida para outras classes que por sua vez podem
	 *  ser instanciadas.
	 * Apenas as classes filhas podem ser instanciadas
	 * 
	 * Para metodos abstract -  as classes filhas pricisam declarar este metodo.
	 * 
	 * */
}
